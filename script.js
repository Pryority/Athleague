
function placeCheckpoint() {
    // getTapLocation();
    // ifCheckpointAtTapLocation();
    // addCheckpointToCurrentCourse();
    // addCheckpointToMap();

    if(checkpoints >= minCheckpoints && checkpoints <= maxCheckpoints) { 
        
    } else if(checkpoints <= 1) { 
        print("You must place at least two checkpoints to create a course.")
    } else if(checkpoints == 49) { 
        print("Checkpoint limit reached.")
    } else { 
        setIdToCheckpointOrder();

   
}

class Checkpoint {
    // var id :
    // let checkpointOrder =
    // let location =
    // var isStart = false;
    // var isFinish = false;


}

function setIdToCheckpointOrder() {
    var checkpoints = [Checkpoint]
}

function createCourse() {
    let checkpoint = Checkpoint
    let checkpoints = [Checkpoint]
    let checkpointCount = Int
    var isSaved = false;
   
    // LOOP THROUGH ALL PLACED CHECKPOINTS
    for(var i = 0; i < checkpointArray.length-1; i++) {

        return 
    }
}

function saveCourse() {
}

function saveCourseToDrafts() {
    // print("Your new course was saved. To edit this course you can find it in your 'Drafts'")
   
}



function loadCourses() {
    var user = new User()
    var myCourses = [Course]
    
}
